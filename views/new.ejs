<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nueva Orden</title>

    <!-- Link de Boostrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container mt-5">
        <!-- Titulo de pagina-->
        <h1 class="mb-4">Crear una nueva orden</h1>
        <!-- Formulario para crer nueva orden, donde los datos ingresados en los campos de texto se envian desde el cliente al servidor por metodo POST-->
        <form action="/orders/new" method="POST" id="orderForm">
            <div class="form-group">
                <!-- TextField para ingresar numero de mesa -->
                <label for="tableNumber">Número de Mesa:</label>
                <input type="number" class="form-control" id="tableNumber" name="tableNumber" required>
            </div>
            <div class="form-group">
                <label for="items">Pedido:</label>
                <div id="items">
                    <div class="form-row">
                        <!-- TextField para ingresar nombre del pedido -->
                        <div class="col-md-4 mb-3">
                            <input type="text" class="form-control" name="items[0][name]" placeholder="Nombre" required>
                        </div>
                        <!-- TextField para ingresar precio del pedido -->
                        <div class="col-md-3 mb-3">
                            <input type="number" class="form-control" name="items[0][price]" placeholder="Precio" step="0.01" required>
                        </div>
                        <!-- TextField para ingresar cantidad del pedido -->
                        <div class="col-md-2 mb-3">
                            <input type="number" class="form-control" name="items[0][quantity]" placeholder="Cantidad" required>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Boton para enviar el formulario al servidor -->
            <div>
                <button type="submit" class="btn btn-primary">Guardar orden</button>
            </div>
        </form>
    </div>
</body>

</html>
